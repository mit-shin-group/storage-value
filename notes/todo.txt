- check that contingency and base case operation sums up to probability (T parameters) --> have T sum to 1 over contingencies
- add option to disallow load shedding in planning problem?
- add fixed daily initial soc based on peak load days into planning problem
- decouple days in planning problem
- change T to a vector of dates in the planning problem
- adjust MIPGap to 0.1%


Results:
- why is the maximum supply from the grid in base case operations so low (38MW), when 74MW would be available?
- why are we exporting to the grid in a peak shaving scenario? -- we are not exporting much.
- why is storage supply greater than storage demand? --> because I was checking max values
- why is grid utilized at full capacity in full market participation mode but not in peak-shaving mode?
- check complementarity of grid im- and exports

Observations:
- the higher the cycle limit, the longer the solution time
- underutilization of the grid

Next step (goal: arrive at a sensible solution):
(done) adjust probabilities (0.8 -- 0.2) 
(done) adjust MIPGap (0.01)
(done) check operational cost of the grid vs backup generation. Why is max grid utilization low in peak-shaving and not in full participation? --> check market participation constraint; at least in the first year all power should be imported from the grid --> summed over g instead of b and s by accident
(done) check if electricity is exported, when it could be used to cover demand --> total electricity exports are about 1/1000th of load, seems negligible, none in updated peak shaving
- experiment with Cs = 0 and full market, to get get investment into power lines

Hypothesis:
- backup generation is too cheap (opex and capex)
--> backup generation: constant at $305/MWh
--> capex: 7.67 million in 2025, 223k in 2050 per MW; sanity check with price for installed genset
- power line expansion is too expensive
--> 52.5 million in 2025, 2.6 million in 2050 per MW; sanity check with price for installed battery
- grid power is too expensive
--> price evolution? (check case data -> psg, constant throughout the years, average $40/MWh)
--> check usage of grid and backup power in base case in 2025, expect (almost) no backup power (check model results) -> 129MWh backup power vs 242,955MWh grid power

Conclusion:
--> opex is fine
--. investigate capex further

Hypothesis:
- glitch in build_data_plan -> no, reads directly from data/nantucket.json
- glitch in data_to_json (writes nantucket.json)
--> current json: backup capital cost 52.5 million (2025) -- 2.6 million (2050)
--> current json: grid capital cost 4.1 million (2025) -- 156k (2050)
--> current json: storage capital cost 7.7 million (2025) -- 223k (2050)

to do:
(done) change "line capital cost" to "cable capital cost", battery -> storage, cable/line -> grid
(done) refine capital cost assumptions
(done) correct capital cost assignment in build_data_plan

Test: rerun julia --project=. code/07_run_plan.jl 
-- on my PC running the planning problem takes over 3850s, while a decent solution is found 1000s.
--> add gurobi time limit for debugging + optimality gap
--> julia +1.11.2 --project=. code/07_run_plan.jl --dates year --market full --cycles 0. --timelimit 900.

Results:
-- solve time limit works
-- much higher costs of lost load in the contingency scenario
-- investing in two new lines only
-- no exports
-- storage is used to profit from negative prices (possible because only discharge cycles are constrained)
-- it seems like more than 2 subsea cables are in service temporarily.
--> check xtot["g", :] and x["g",:], indeed the 36MW cable is replaced in 2037 and the second cable is laid in 2038, several years before the 38MW cable reaches the end of its lifetime at the end of 2046.

Conclusion:
--> cost of lost load is too low to drive investment into third subsea cable/sufficient backup generation to cover demand, even in the base case 

to do:
--> rerun with value of lost load $10,000/MWh, set parameter in data_to_json.jl

Results:
-- no lost load in base case
-- unmet load in contingency case reduced by a factor 5
-- invest into three lines only
-- x["g", :]: build in 2036, 2037,and 2047

Ways forward:
-- accept this as base case and run planning problem with storage, which may result in some storage installation as storage is cheaper than backup power
-- do not accept this as in the absence of storage, the utility would probably want to withstand the contingency event --> expect to see either investment in a 4th cable or in backup generation.

--> increase contingency probability to 50%

Results:
-- decrease lost load by a factor 8, by installing cable upgrades earlier. Still, no investment in backup generation.

Question:
- why no investment in backup generation?

--> check backup generation prices (case_data)

Alternative: run with higher value of lost load

----------
23 June 25 (backup deployment)

Observation:
-- There is no backup capacity in xtot for contingencies --> that's correct, because I'm only using xtot for base case for backup generation and storage. Also apparent from storage deployment when backup generation had lower costs.
-- Backup generation price is constant at $305/MWh -- compare to 0.50€/kWh = 500€/MWh, this makes generation price makes sense.
-- Investment cost: from $53/W to $2.6/W, i.e., 15MW would cost 80 million, it seems like this cost is twice as high as it should be
--> I forgot to normalize by diesel capacity, so the per-unit capital cost was about 10 times higher than it should have been.
--> new investment cost is $4.1/W, similar to grid expansion capital cost, but more modular

Test:
julia +1.11.2 --project=. code/07_run_plan.jl --dates year --market full --cycles 0. --timelimit 900.

Way forward:
-- decrease contingency probability
-- increase MIPGap to 0.2% for prototyping

Result:
-- still no investment in backup generation

--> run with higher value of lost load (20k is not high enough)

Operating cost comparison:
-- Backup generation: $305/MWh
-- Grid: $40/MWh

Observation:
-- with Voll at 50k, investment into 4th line and reduction in total lost load from 4.6GWh to 2.8GWh up to 3.3GWh at 100k VOLL --> why?
--> seems like MIP tolerance is the issue

Way forward:
-- run with no load shedding --> convert inequality to equality constraint

--> julia +1.11.2 --project=. code/07_run_plan.jl --dates year --market full --cycles 0. --timelimit 900. --load_shedding false

Observation:
-- without load shedding, no load is shed (good)
-- 80MW investment in line upgrades, 58MW in backup generation

Question
-- how does the solution depend on the optimality gap?
-- and on the contingency probability?

Next steps:
-- run with 0.1% gap --> leads to reduced backup generation investments (120MW grid vs 22MW backup)
-- run with 0.01% gap --> timed out
-- run with contingency 10% and base case 90% and 0.1% gap (80MW Grid, 80MW backup)
-- run with contingency 20% and base case 80% --> works nicely

Overall result for today:
-- corrected backup generation capital costs
-- implemented no-load-shedding option
-- found reasonable base vs contingency probabilities

Next steps:
run with different cycle limits and for peak shaving, use sbatch for this

sbatch code/07_run_plan.sh --dates year --market full --cycles 0

sbatch code/07_run_plan.sh --dates year --market full --cycles 150
sbatch code/07_run_plan.sh --dates year --market full --cycles 366
sbatch code/07_run_plan.sh --dates year --market peak_shaving --cycles 0
sbatch code/07_run_plan.sh --dates year --market peak_shaving --cycles 150
sbatch code/07_run_plan.sh --dates year --market peak_shaving --cycles 366

----------
24 June 25 (storage deployment)

peak shaving 150 is still running, peak shaving 366 had not started overnight
I ran them with load shedding, but wanted to run without load shedding.

Observation:
(full market)
-- 0 cycles: Grid 80MW, backup 30MW
-- 150 cycles: Grid 120MW, backup 0MW
-- 366 cycles: Grid 120MW, storage 8MW, but higher optimality gap and slightly lower objective value than with 150 cycles --> not trustworthy
-- NAN for storage supply cycles even when battery is present and number of allowed cycles > 0
(peak shaving)
-- 0 cycles: Grid 120MW, backup 10MW, slightly lower optimality gap and higher objective value than with full market participation
-- 150 cycles: Grid 0MW, backup 0Mw, storage 2MW, 40% optimality gap even after running for 8 hours
-- 366 cycles: takes 10,000+s to reach an optimality gap of 40%.

Conclusion:
-- (near-)optimal solution landscape seems flat
-- peak shaving with storage is currently intractable

Next steps:
(done) check on progress of peak shaving with 366 cycles to see if the cycle constraint causes slow down --> seems worse/similar
(done) fix NAN for storage cycles --> must be in 04_analysis.jl, run the print file on login node to debug --> NAN appears when a year has zero storage capacity
(in progress) rerun experiments with no load shedding 
-- fix state-of-charge at the beginning of each day to a single decision variable
-- provide no cycle constraint option

sbatch code/07_run_plan.sh --dates year --market full --cycles 0 --shed false
sbatch code/07_run_plan.sh --dates year --market full --cycles 150 --shed false

remember to prototype with planning for peak days only, where applicable.

full market participation without load shedding is also slow

Allowing for active storage almost doubles solution time, even for full market participation, i.e., the LP case.

Fixing state-of-charge
--> adjust build_model and data format: introduce equality constraints or have a function that adds operational problems for given days (daily decomposition)

Data structure: dense axis array or dict? --> dict would allow to consider 23h and 25h days but be inconsistent with the rest of the model.

Decision: go with dense axis array and neglect days with daylight savings time switches
--> will require new benchmarking. Include option of time coupling into the optimization model.

Summary for the day:
-- implemented ybar_ell as a dense axis array with days as an index
--> continue with pg

overnight run full with 366 cycles

----------
25 June 25 (daily decomposition)

check on overnight run

Observation:
-- large speed-up by imposing no load shedding: from 12,200s to 700s albeit at slightly worse optimality gap (0.1% instead of 0.07%).
-- large speed-up by relaxing cycle limit: from 7,700s to 700s 
-- resulting capacity mix: 120MW cable expansion, 20MW storage, no more backup investment compared to 150 cycle case.

Continue with independent day implementation and perform tests on full market participation.

(done) adjusted data format

question: how to get day-labels from DenseAxisArray
days = [k[1] for k in keys(case_data.ȳℓ[2025,:,1])]

Correction: I had not imposed a proper capacity limit on the state-of-charge! -- only on the terminal state-of-charge (maybe a copilot error since I had the proper .<= syntax)


remains to complete market participation

with separation by day, model building is slower (31 seconds)

use time to look into Benders or bundle methods?

Observation:
-- with daily decomposition and bound on the state-of-charge: 0 cycle case shows lower surplus and almost 10 times longer run time. Why?
--> contingency costs show big difference

Hypothesis:
--> battery is charged less? But, no, we already had a bound on the terminal state-of-charge previously.
--> check storage demand, did indeed vanish because we now have an equality constraint on the terminal state-of-charge, 780MWh originally, how much revenue does that contribute --> $19.5k in both base case and contingency
--> makes sense that's limited because it requires negative prices.

--> does not explain the 170 million drop in surplus

----------

26 June 25 (contingency costs)

from yearly problem with no upper bound and 0 cycles to 0 cycles with daily decomposition and upper bound to 150 cycles:

Backup investment: 25MW -> 82MW ->  13MW
Grid investment:  120MW -> 80MW -> 160MW
Storage investment: 0MW ->  0MW ->  17MW
Contingency operating cost: $503M -> $333M

Terminal capacity of 160MW? --> why is storage still needed? --> just for arbitrage? But arbitrage should induce peak-shaving and 120MW cable should be enough.

Problem with prototyping is the time each optimization run takes; between 700 and 7000s even without market participation constraints.

Include option to consider every other day?

--> modify data generation, mostly costs?

----------

02 July 25 (stride and Benders: does that work with storage interday coupling through cycling limit -- feasibility cuts?, maybe also MIPFocus)

experiment: run planning script with stride=2

Variables:
1,598,974 continuous, 78 integer (78 binary) --> as expected, reduced number of continuous variables by half, runs in 551s.

fix data generation: modify not costs but Tnc, multiply by stride, valid as long as the total number of days is divisible by stride --> correcting now.

with stride = 5 --> 57s --> much easier to experiment with

----------

07 July 25 (stride)

(done) also need to adjust constraints, in particular the cycle limit, ideally adjust this in data generation as well; adjusted cycling constraints to avoid adjusting Cs

check the solution:

- Speedup from 7200s to 275s for 150 cycles full market participation wiht stride = 5.
- Double the operating costs, base case has higher operating cost than contingency? --> corrected
- Similar capital cost
- No storage, only line and backup generation build-out
- Max demand is a bit lower --> stride did not include the peak day
- No backup supply in base case 
- Similar base-case demand and operating costs in base case
- heavier reliance on backup, lower reliance on storage in contingency
- similar contingency operating costs

to do:
(done, parenthesis was missing) check reporting of total operating costs

new experiments:
1. full, zero cyle limit: 51s but of course no storage operations
2. full, 365 cycles: 358.35s, compared to 1500s previously --> triggers storage investment
3. full, 150 cycles, 12 stride, hope to get something close to a minute for prototyping --> 80s (close enough), triggers some storage investment, overall costs are quite similar.
3. peak shaving, zero cycles (sbatch)
4. peak shaving, 150 cycles (sbatch)

printing file is correct

peak_shaving runs failed --> update code logic for strided data.

----------

08 July 25 (stride for peak shaving)

Big-M computation is the challenge. Get tighter bounds with daily decomposition? --> yes, somewhat. Bounds can be further tightened by forbidding load shedding and accounting for asset lifetimes.

Trial run with stride 48, required some tuning of arguments in the peak shaving function.

Result: prefers to invest in backup generation, rather than storage

test cases: stride: 24, 12, 6, 4, 3, 2

::::::::: Benders




